<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NoteHub</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <h1 class="text-3xl font-bold mb-6">NoteHub</h1>
            <form action="/create" method="post">
                <input type="text" placeholder="Title Goes Here..!" name="title" id="title" class="w-full p-2 rounded-md bg-zinc-800 block border-2 border-zinc-700 text-white">
                <textarea name="details" id="description" placeholder="Description Goes Here..!" class="w-full p-2 mt-2 rounded-md bg-zinc-800 block border-2 border-zinc-700 resize-none text-white"></textarea>
                <input type="submit" value="Create Note" class=" p-2 rounded-md bg-zinc-800 text-white mt-2 hover:bg-zinc-700 transition-all duration-300">
            </form>
        </div>
        <div class="tasks flex gap-3 p-10">
            <% if(files.length>0){ %>
                <% files.forEach((tasks)=>{ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">

                        <h1 class="text-white text-xl tracking-tighter"><%= tasks.replace(".txt","") %></h1>
                        <div class="flex w-full justify-between">
                            <a href="/files/<%= tasks %>" class="text-blue-500 rounded-md mt-3 inline-block text-sm">Read More.</a>
                            <a href="/edit/<%= tasks %>" class="text-zinc-600 rounded-md mt-3 inline-block text-sm">Edit FileName</a>
                        </div>
                    </div>
                <% }) %>
            <% } else{ %>
                    <h3 class="text-white text-xl tracking-tighter">No Notes Yet..</h3>
            <% } %>
            
        </div>
    </div>
</body>
</html>